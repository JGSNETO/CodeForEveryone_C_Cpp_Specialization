/*
 A lambda expression in C++ is a way to define an anonymous function directly in your code. it allows you to write short, inline functions withou needing to declare a separate
 named function. Lambdas are particularly useful for passing functions as arguments to algorithms or ecent handles. 

Basic sintax:

[capture](parameters) -> return_type {
    // function
};

- Regular functions cannot be defined inside int main(). But lambda functions can !
*/